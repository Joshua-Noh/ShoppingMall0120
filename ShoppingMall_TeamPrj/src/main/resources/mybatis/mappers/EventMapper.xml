<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
   "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.shop.ShoppingMall_TeamPrj.events.mapper.EventMapper">

    <!-- 이벤트 ID로 이벤트 조회 -->
    <select id="getEventById" parameterType="int" resultType="com.shop.ShoppingMall_TeamPrj.events.vo.EventVO">
        SELECT * FROM events WHERE event_id = #{eventId}
    </select>

    <!-- 모든 이벤트 목록 조회 -->
    <select id="getAllEvents" resultType="com.shop.ShoppingMall_TeamPrj.events.vo.EventVO">
        SELECT * FROM events
    </select>

    <!-- 새로운 이벤트 삽입 -->
    <insert id="insertEvent" parameterType="com.shop.ShoppingMall_TeamPrj.events.vo.EventVO">
        INSERT INTO events (event_name, event_description, event_type, start_date, end_date)
        VALUES (#{eventName}, #{eventDescription}, #{eventType}, #{startDate}, #{endDate})
    </insert>

    <!-- 기존 이벤트 업데이트 -->
    <update id="updateEvent" parameterType="com.shop.ShoppingMall_TeamPrj.events.vo.EventVO">
        UPDATE events
        SET event_name = #{eventName},
            event_description = #{eventDescription},
            event_type = #{eventType},
            start_date = #{startDate},
            end_date = #{endDate}
        WHERE event_id = #{eventId}
    </update>

    <!-- 이벤트 삭제 -->
    <delete id="deleteEvent" parameterType="int">
        DELETE FROM events WHERE event_id = #{eventId}
    </delete>

</mapper>
