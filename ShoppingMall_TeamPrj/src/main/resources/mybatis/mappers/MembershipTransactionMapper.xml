<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
   "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.shop.ShoppingMall_TeamPrj.membership.mapper.MembershipTransactionMapper">

    <!-- 사용자별 포인트 거래 내역 조회 -->
    <select id="getTransactionsByUserId" parameterType="int" resultType="com.shop.ShoppingMall_TeamPrj.membership.vo.MembershipTransactionVO">
        SELECT transaction_id, user_id, points_change, transaction_type, transaction_date, description
        FROM membership_transactions
        WHERE user_id = #{userId}
    </select>

    <!-- 포인트 거래 내역 삽입 -->
    <insert id="insertTransaction" parameterType="com.shop.ShoppingMall_TeamPrj.membership.vo.MembershipTransactionVO">
        INSERT INTO membership_transactions (user_id, points_change, transaction_type, transaction_date, description)
        VALUES (#{userId}, #{pointsChange}, #{transactionType}, #{transactionDate}, #{description})
    </insert>

</mapper>
